const botName = 'OllieBot';
const appName = 'olliebot-js';
const ownerID = '';
const prefix = '.';
const token = '';
const databasePath = 'test/olliebot.db';
const logSize = 50 * 1024;
const feedsMax = 3;

const googleApiKey = "";

export {
  botName,
  appName,
  ownerID,
  prefix,
  token,
  databasePath,
  logSize,
  feedsMax,
  googleApiKey
}
